export interface Roles {
	verificador?: boolean;
	observador_general?: boolean;
	solucionador?: boolean;
	modificador?: boolean;
	agregador?: boolean;
	creador?: boolean;
	observador?: boolean;
	interventor?: boolean;
	gestor?: boolean;
}
